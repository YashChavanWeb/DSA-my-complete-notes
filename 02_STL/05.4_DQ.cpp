#include<bits/stdc++.h>
using namespace std;

int main() {

   deque<int> dq;

   dq.push_back(3);
   dq.emplace_back(2);

   dq.back();
   dq.front();

   // rest all functions are same as vector
//    begin, end, rbegin, rend, clear, insert, size, swap 

    return 0;
}